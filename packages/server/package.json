{
  "name": "@increa-reader/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python server.py",
    "start": "python server.py",
    "install:python": "pip install -r requirements.txt",
    "install:dev": "pip install -e '.[dev]'",
    "test": "pytest",
    "test:cov": "pytest --cov=increa_reader --cov-report=term-missing"
  }
}
